# Supabase — server-side only.
#
# SUPABASE_SERVICE_ROLE_KEY bypasses row level security. It must never be
# prefixed NEXT_PUBLIC_, which would ship it to every visitor's browser.
# Set both in Vercel's environment variables, not in a committed file.
SUPABASE_URL=https://nfpzpnbshhhdxhbaywdg.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# The canonical origin, used for metadata, sitemap and Open Graph.
NEXT_PUBLIC_SITE_URL=https://mesub.io
